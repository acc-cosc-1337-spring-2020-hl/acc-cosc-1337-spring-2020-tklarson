//Write function prototype for a value-return function named gross_pay that accepts a
//double parameter named hours and a double parameter named hourly_rate.  The function
//returns a double which is the product of hours and hourly_rate.

#include<iostream>

using namespace std;

int gross_pay(int h, int hourly_rate)
{
	if (h <= 40);
	{
		int regular_pay = (h*hourly_rate);
	}
	else (h >= 40);
	{
		int overtime_rate = ((h-40)*(hourly_rate*1.5));
	}

	int gross =

	return gross;
}
